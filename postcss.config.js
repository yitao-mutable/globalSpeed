
module.exports = {
  plugins: [
    require('postcss-nested'),
    require('autoprefixer')
  ],
  closeButton: require('closeButton')
}

module.exports = {
  plugins: [
    require('postcss-nested'),
    require('autoprefixer')